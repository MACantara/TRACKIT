# Flask Application Configuration
# Generate a secure random key for production using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here

# Database Configuration
# SQLite database file path (relative or absolute path)
# Example: sqlite:///trackit.db (creates trackit.db in the project root)
DATABASE_URL=sqlite:///trackit.db
